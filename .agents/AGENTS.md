# Code Change Transparency Rule

Whenever modifying any file (adding, editing, or deleting code):
1. Always display a clear summary of the changes.
2. Always present the exact code diff (`diff` block) showing added (`+`) and removed (`-`) lines.
3. Allow the user to review the diff to easily accept or request revisions.
